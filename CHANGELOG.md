# 更新日志

本项目的所有重要变更都会记录在此文件中，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)规范，并与 git 标签（如 `v1.0.0`）保持一致。

## [v1.0.0] - 2026-09-18

首个正式发布版本。

### 新增

- 自动检测并列出所有已插入且可访问的可移动 U 盘
- 基于文件后缀的备份策略配置（`config.ini`）：不同后缀可配置复制（copy）/ 移动（move）/ 删除（delete）操作
- 两种目标子文件夹命名规则：
  - `every_day`：按每个文件自身的修改日期归档（`YYYY.MM.DD`）
  - `every_time`：按本次备份文件的整体时间范围生成统一子文件夹，支持附加自定义名称
- 增量备份：复制完成的文件记录于 U 盘本地数据库，重复备份自动跳过
- 同名文件智能处理：目标已存在同名文件时，大小相同视为同一文件自动跳过；大小不同自动追加数字后缀（`photo_1.jpg`），不覆盖任何已有文件
- 多 U 盘合并处理：一次性扫描 / 备份 / 弹出所有 U 盘，`every_time` 类型文件跨 U 盘共享统一时间范围子文件夹
- 每个 U 盘在 `.auto_backup_data/sqlite.db` 中维护文件索引与备份日志，实现去重与审计
- 通过 Windows DeviceIoControl API 安全弹出 U 盘
- 交互式多级命令行菜单，实时显示 U 盘初始化 / 扫描状态与备份统计
- 备份路径支持 `%VAR%` 环境变量与 `~` 前缀展开
- PyInstaller 打包脚本（`scripts/build_exe.py`）：生成含说明文件的发布文件夹与带版本号的 zip 发布包
